# Solace PubSub+ Broker Configuration
SOLACE_BROKER_URL=ws://localhost:8008
SOLACE_VPN_NAME=default
SOLACE_USERNAME=default
SOLACE_PASSWORD=default

# Server Configuration
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Admin Authentication
ADMIN_PASSWORD=admin123

# AI Configuration
# AI settings are configured per-session through the Admin UI.
# Click the "AI Settings" button in the admin panel to configure your AI provider.
# Supported providers: LiteLLM, OpenAI, Anthropic Claude
